<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Simple Chat</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbar-1" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbar-1">
        <% if current_user %>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">
                    <%= link_to (I18n.t 'navbar.user.edit'), edit_user_registration_path, :class => "nav-link" %>
                </li>
                <li class="nav-item active">
                    <%= link_to (I18n.t 'navbar.user.logout'), destroy_user_session_path, :method => :delete, :class => "nav-link" %>
                </li>
            </ul>
        <% else %>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">
                    <%= link_to (I18n.t 'navbar.user.login'), new_user_session_path, :class => "nav-link" %>
                </li>
                <li class="nav-item active">
                    <%= link_to (I18n.t 'navbar.user.register'), new_user_registration_path, :method => :delete, :class => "nav-link" %>
                </li>
            </ul>
        <% end %>
    </div>
</nav>